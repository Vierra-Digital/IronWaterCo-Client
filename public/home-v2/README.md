# Home V2 media assets

Optional assets for the scrollytelling homepage (`/home-v2`).

| File | Purpose |
|------|---------|
| `hero-cinematic.mp4` | 10–15s muted loop for hero background (luxury bathroom). When present, replaces Ken Burns placeholder. Poster: `/hero-showerscape.png`. |
| `section-plumbing.jpg` | Optional parallax still |
| `section-lighting.jpg` | Optional parallax still |

Until `hero-cinematic.mp4` exists, the hero uses animated Ken Burns on existing PNGs plus a WebGL steam particle overlay.
